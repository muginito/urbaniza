name: Continuous Integration

on: 
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  continuous-integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Action Node.js
        uses: actions/setup-node@v4
        with: 
          node-version: 16.x

      - name: Install dependencies
        run: npm install @commitlint/{cli,config-conventional} husky
          
 #     - name: install, build, test
 #       run: |
 #         npm install
 #         npm run build
 #         npm run test
